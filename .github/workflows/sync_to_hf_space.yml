name: Sync to Hugging Face Space

on:
  push:
    branches: [main]  # Trigger when you push to main branch
  workflow_dispatch:  # Allow manual triggering

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Sync with Hugging Face Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "moinuddinnavalur6@gmail.com"
          git config --global user.name "moinuddin09"
          git remote add hf https://huggingface.co/spaces/Moinuddin-123/AI-News-Researcher
          git push hf main

